# Force LF pour tout ce qui doit être cross-plateforme
* text=auto eol=lf

# Unreal Engine assets always use LFS
*.uasset filter=lfs diff=lfs merge=lfs -text
*.umap   filter=lfs diff=lfs merge=lfs -text
